<svg
  ><filter id="noise"
    ><feTurbulence type="fractalNoise" baseFrequency=".8"></feTurbulence>
  </filter><rect width="100%" height="100%" filter="url(#noise)"></rect></svg
>

<style>
  svg {
    position: fixed;
    inset: 0;
    z-index: 9999;
    width: 100dvw;
    min-height: 100dvh;
    opacity: 0.2;
    pointer-events: none;
    transform: translateY(0);
    filter: contrast(10%) brightness(100%);
  }
</style>
